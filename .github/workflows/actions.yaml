name: GitHub Actions
on:
  pull_request:
    branches:
      - main
  push:
    branches:
      - main
env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
# Static site: build locally, commit dist/, push. Hostinger serves committed dist/.
# No CI jobs needed - type checking done locally in Cursor.
